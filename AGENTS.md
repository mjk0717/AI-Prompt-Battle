# AGENTS.md

- Always preserve file encoding.
- For text files, prefer UTF-8.
- Never rewrite a file if its encoding is unclear.
- On Windows, explicitly use UTF-8 when reading/writing files.
- Do not change line endings unless necessary.
- Do not use `localStorage` in this project.
